from setuptools import setup, find_packages


setup(
    name="calculator",
    version="1.0",
    packages=find_packages(),
    description='Calculate that evaluate postfix and infix notations.',
    author='Ehab Suleiman'
)